import React, { Component } from 'react';
interface IProps {
    title?: React.ReactNode;
    content?: React.ReactNode;
    action?: React.ReactNode;
}
declare class WorkflowTitle extends Component<IProps> {
    render(): {};
}
export default WorkflowTitle;
